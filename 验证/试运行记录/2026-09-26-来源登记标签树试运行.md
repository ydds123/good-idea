# 试运行记录｜2026-09-26｜来源登记（标签树）

- 日期：2026-09-26
- 类型：来源登记（规则层改动）
- 关联内容：无新增外部来源；涉及 `规则/标签树.md`（新建）、`规则/README.md`、`规则/内容对象.md`、`规则/协作协议.md`、`规则/默认规则.md`、`规则/规则决策记录.md`、`数据/模板/source.md`，工具 `sync_source.py`、`goodidea_weekly_sync.py`
- 完整对话位置：本记录下方
- 松海目标：① 外部来源要有标签属性，表达这份材料跟什么相关；② 打标签不能随便打，规则层要有标签树，匹配就用树内节点，没有就先在树上创建再引用；③ 标签树的设计参考 flomo（浮墨笔记）关于标签的设计思想

## 完整可见对话

**松海（14:0x）**

没有。我觉得外部来源还是需要有一个标签属性，来表达这个内容跟什么相关。比如这个，它其实就是和“炒饭会”这个标签相关，对吧？

关于打标签，也不是随便打。最好在规则层有一个标签树。如果这篇内容跟标签树里的内容匹配，就用标签树的内容；没有的话，先在标签树创建，然后再引用标签树的内容。

**松海（补充，同一轮）**

注意标签树的设计。我希望你参考 Flomo，也就是浮墨笔记里关于标签这块的设计思想。

**Agent（执行）**

按 flomo 的设计思想重写标签树，并把它接进既有链路：

1. 先取 flomo 官方 flomo 101 的原始说法（「如何规划标签」「用多级标签，轻松分类」）：标签是索引不是分类；标签生来不平等（导航 / 线索）；多级标签用 `#标签/子标签/孙标签`；一条 MEMO 可以多个标签；结构从使用中生长，一开始不要铺多级；用频率看标签、不做无谓整理。
2. 据此写 `规则/标签树.md`：两类标签（导航 / 线索）、层级最多两层且从一级长起、一份材料可挂多个、不追求互斥穷尽、不按单篇材料的临时话题建节点、不做无谓整理、命名不用 emoji 与特殊字符；当前节点只有 `炒饭会`（导航）。
3. 只传设计思想、不照抄少楠个人的 P.A.I.R 骨架——flomo 官方自己写明「不要照抄这套方法，而是理解为何这么设计」。
4. 按单一归属分配各处文字：允许值 → `标签树.md`；字段语义 → `内容对象.md#外部来源`；登记时怎么判断与引用 → `协作协议.md#统一来源登记协议` 新增第 3 步；识别炒饭会材料的可调默认 → `默认规则.md`；格式示例 → `数据/模板/source.md`；原因 → `规则决策记录.md`；`规则/README.md` 文件职责表加一行。
5. 工具侧：`--tag` 的取值按标签树校验（树外标签在报告里告警），weekly 判据换成「自己录的音 + 周三 17:00 后开始 + ≥20 分钟 + 非工作会议」，新增 `tags` 体检模式（使用次数 / 树外标签 / 闲置节点）。

**实测（本机，2026-09-26）**

| 检查 | 结果 |
|---|---|
| `weekly --scan-only --since 2026-06-15`（85 条笔记） | 自动 17 条 = 13 个周三晚（06-24 ~ 09-23），疑似 1 条（07-20 上海线下课，正文提到「炒饭会」），跳过 67 条 |
| 标签校验 | 树内 `炒饭会` 通过；`不存在的标签` 在报告里告警 |
| 一周干跑（since 09-21） | 自动 1 条：OPC 一人公司分享（09-23 19:54、179 分钟），生成的 frontmatter 含 `tags: ["炒饭会"]` |
| `tags` 体检 | 19 份记录：暂无标签；树内节点 `炒饭会` 闲置 |
| 标题兜底去重 | 08-12 的笔记命中既有记录（以 08-13 登记，source 是 d.biji 分享短链），状态 `exists_title`，不重复登记 |

修掉的三处缺陷：① 脚本 `from datetime import date, timedelta` 后误用 `datetime.datetime.fromisoformat` → 改为 `datetime.fromisoformat`；② 周例包装脚本的条件续行写坏（`SyntaxError`）→ 改写；③ `print_report` 不打印报告级告警 → 补上。

## 观察

- 顺畅之处：标签树的取值来源、字段语义、登记流程、识别默认、格式示例各自只落一处，没有复制第二套规则；工具只做校验与统计，判断留给松海/Agent。
- 打断或多余步骤：新判据第一次跑出 `auto=0`，排查两层——先是导入写法（类与模块同名），后是示例解析；两次都在脚本内定位，未动规则。
- 用户纠正：松海把「标签字段要不要进格式」升级为「规则层要有标签树」，并指定设计参考 flomo；此前准备的三处最小改动提案作废，改为按标签树机制重写。
- 候选忠实度：不适用（未产生闪念或永久卡片候选）。
- 召回相关性与噪声：读 flomo 官方文档 2 篇（标签规划、多级标签）＋社区文章 1 篇；未引入 P.A.I.R 等未落地骨架。
- 失败降级：标签校验只告警不阻断；脚本不会自动往规则文件写节点（写树是 Agent 的动作，需单独提交）。

## 结果

- 行动（含当前 `status`）：无
- 闪念卡片（含当前 `status` 与 `## 去向`）：无
- 永久卡片：无
- 外部来源：0 新增（待定候选：10 场周三晚的炒饭会材料，含 09-23 OPC）
- 零产出：否

## 判断

- 问题层级：规则（标签的取值来源、字段语义、登记时如何判断）
- 当前处理：按松海指示改规则层（新增 `标签树.md` 并接通四处引用），未决部分（是否补登近 10 场、一晚多段录音怎么处理）留给他定
- 证据与理由：13 个周三晚全部命中且无误报（85 条实测）；关键词命中率低（13 场仅 2 场），故只作确认信号；已登记的 19 份记录暂无标签，回填需要单独决定
