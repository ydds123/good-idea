# 试运行记录｜2026-09-26｜来源登记（周例与标签）

- 日期：2026-09-26
- 类型：来源登记
- 关联内容：无（本轮未新增外部来源；相关工具：Hermes 技能 `goodidea-source-sync`、`~/.hermes/scripts/goodidea_weekly_sync.py`）
- 完整对话位置：本记录下方
- 松海目标：明确脚本的定位与通用性，并要求两件事——① 松海说同步时由 Agent 调用脚本完成；② 每周三或周四，把前一天与「炒饭会」相关的内容同步进外部来源并打标签「炒饭会」（他给出识别特征：时长通常 2 小时左右）

## 完整可见对话

**松海（13:49）**

注意，你这个脚本有什么用？

我希望是这样的：
1. 当我让你获取笔记同步内容时，你能够调用这个脚本，把我需要同步的东西同步到外部来源。
2. 每周三，或者每周四的时候，把前一天和炒饭会相关的内容进行同步。同步时打上标签“炒饭会”。

炒饭会的播客特征在于，它的时长通常在 2 小时左右。

你这个脚本具备一定的通用性吗？

**Agent（最终报告）**

先回答你的两个问题。

## 1. 这个脚本是什么（有什么用）

它是「外部来源登记」这个动作的固定入口：给它一个得到大脑的笔记（id 或链接），它**一次调用**完成取材料 → 按数据层格式组装正文 → 图片入库 → 去重 → 落盘 → git 提交，并输出一份 JSON 报告。你不用它也能做，用它就是把原来 19 个模型回合的手工流程压成 1 次调用：单篇 1~2 秒、10 篇批量约 15 秒。

**你的第一条要求（你说同步，我调用它）现在就是这样**：你说「把得到大脑里那篇 X 放进外部来源」，我跑 `sync_source.py sync <id> --commit`；一次给多个 id 也行。

## 2. 你的第二条要求（每周三/四同步前一天的炒饭会内容并打标签）

拆成三部分，前两部分已落地，第三部分卡在规则上、需要你点头：

| 部分 | 状态 |
|---|---|
| 时段 | 已建两条 cron：周三 23:30、周四 09:00；结果发到你飞书（与读书陪伴同一个会话），没有内容就不发消息 |
| 识别 | 已实现，并用你 9 月的真实笔记校准过（见下） |
| 标签 | 脚本已支持 `--tag 炒饭会`，但「外部来源能不能带标签字段」规则里没有答案，需要你定 |

## 3. 识别口径（实测校准，不是拍脑袋）

- **自动同步**：标题或智能笔记正文出现「炒饭会」→ 9 月命中 2 条：炒饭会两百期复盘（148 分钟）、OPC 一人公司分享（179 分钟，未登记）。
- **疑似（只报告、等你确认）**：时长 90~210 分钟 **且** 内容类型含 播客/分享/沙龙/课程/访谈 → 命中 Huberman Lab #728（172 分钟，播客访谈）。
- **排除**：内容类型「工作会议」（122、152 分钟的两场项目会）、企业微信录音、9~59 分钟的短视频段。

**一个发现：单看时长不足以识别炒饭会。** 同一段时间里，122 分钟和 152 分钟的都是「工作会议」，Huberman 是 172 分钟的公开播客，只有 148 分钟的 200 期复盘是真炒饭会。所以我用关键词做主判据、时长+类型做「疑似」提示，不自动打标签——否则会把别人的播客标成炒饭会。

## 4. 标签要你定的一处规则（最小改动，三处）

- 现状：`数据/模板/source.md` 的 frontmatter 只有 `source`（必需）＋可选 `author`、`published_at`；`规则/内容对象.md#外部来源` 在 09-25 刚被收缩成「客观属性与实际内容，不记录获取过程」。标签没有归属，按 AGENTS.md 我不能让实现自己补成规则。
- 建议改动：① 模板加一行注释示例 `# tags: ["炒饭会"]`；② 内容对象那条既有句子上补一个词——「作者、发布日期**和材料所属系列标签**能够可靠确定时保留」；③ 规则决策记录加一条 2026-09-26 的原因。
- 你回「可以」我就写这三处，并把 cron 切到 `--tag 炒饭会`（已同步的会在下次运行时顺带补上标签）；回「不用字段」，我就把标签降级成报告里的分组信息，记录格式不动。

## 5. 顺带两件

- 09-23 的 OPC（179 分钟，命中关键词、未登记）要不要顺手登记？Huberman 那条是公开播客不是炒饭会，要不要作为普通外部来源登记（不带标签）？
- 试运行记录已写：`验证/试运行记录/2026-09-26-来源登记周例与标签试运行.md`。

## 观察

- 顺畅之处：在已实测通过的脚本上做增量（新增 weekly 模式与标签支持），用「与已提交记录逐字节一致」做回归，确认没破坏既有行为；周例接成 no_agent cron，窗口内无内容时输出为空、不推送。
- 打断或多余步骤：为校准识别口径，额外取回 6 条真实笔记比对时长与内容类型，才发现「2 小时」不是充分条件（122、152 分钟为「工作会议」，172 分钟为公开播客）。
- 用户纠正：松海把使用方式从「单次为我服务」纠正为「① 我随时触发 ② 每周三/四固定节奏」，并给出炒饭会时长特征。
- 候选忠实度：不适用（未产生闪念或永久卡片候选）。
- 召回相关性与噪声：读规则 2 份、规则决策记录 1 段、同类先例 3 份；未逐份重建。
- 失败降级：本轮修掉脚本 3 处缺陷（`--tag` 参数名不一致、分页游标传成整数、疑似名单里混入已登记项）；标签字段因缺规则归属而**主动不落盘**，改为向松海提议，由他决定。

## 结果

- 行动（含当前 `status`）：无
- 闪念卡片（含当前 `status` 与 `## 去向`）：无
- 永久卡片：无
- 外部来源：0 新增（候选 2 条待定：OPC 一人公司分享、Huberman Lab #728）
- 零产出：否（产出的是工具与自动化，不是内容对象）

## 判断

- 问题层级：可调默认（标签字段的归属）＋协议（周例成为固定节奏后需要写进协作顺序）
- 当前处理：提议调整（标签字段等松海确认；确认前 cron 不带标签，只同步）
- 证据与理由：时长不足以识别炒饭会（122/152 分钟工作会议 vs 148 分钟炒饭会 vs 172 分钟公开播客），故以关键词为主判据；标签字段没有规则归属，按 AGENTS.md「不由实现自行补成规则」处理
