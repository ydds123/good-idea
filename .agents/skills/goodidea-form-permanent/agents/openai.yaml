interface:
  display_name: "Good idea 提交用户卡片"
  short_description: "只接收并原样提交用户亲自写成的永久卡片草稿"
  default_prompt: "Use $goodidea-form-permanent to submit my own permanent-card draft without rewriting it."
