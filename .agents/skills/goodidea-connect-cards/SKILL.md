---
name: goodidea-connect-cards
description: "判断 Good idea 正式卡片是否已接入卡片网络，并在存在合适对象时提出、确认语义连接。用户询问卡片关系、希望接入卡片盒、建立支持冲突演化或例证关系时使用；允许首张卡片以零连接节点入网，实际连接先展示理由，用户确认后才写入双向链接。"
---

# 连接卡片

正式卡片被接纳并进入永久空间、索引和状态账本时，就已经成为卡片网络节点。卡片网络允许从一个没有语义边的节点开始；没有其他正式卡片，或没有值得建立的关系时，报告“已入网，当前零连接”即可，不调用 `connect propose`，也不为了完成流程制造表面关系。

只有存在另一张合适的正式卡片时，才进入连接流程。读取两张正式卡片及其来源。关系必须表达可解释的语义，例如支持、冲突、限定、例证、演化、行动验证或索引入口，不能只因关键词相同而连接。

## 两阶段流程

1. 先判断是否存在合适的另一张正式卡片；不存在时结束并报告当前为零连接节点。
2. 存在合适对象时，说明候选关系、方向和理由。
3. 调用候选命令：

       uv run goodidea --root <仓库> connect propose --from-id <ID> --to-id <ID> --relation <关系> --rationale <理由>

4. 向用户展示候选并等待明确确认。
5. 确认后调用：

       uv run goodidea --root <仓库> connect accept --proposal-id <ID>

6. 运行 goodidea verify，确认双向链接、连接账本和索引一致。

不要自动接受连接，不要创建只停留在关键词层面的表面关系。零连接不是错误，也不影响正式卡片完成入网。
