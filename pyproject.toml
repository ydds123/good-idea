[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "goodidea-v016"
version = "0.16.0"
description = "A simple and flexible rule-first cognition system"
requires-python = ">=3.13,<3.14"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["kernel/goodidea"]
