# Good Idea 闪念捕获优化实施发现

## 已确认事实

- 当前路由把“带链接或外部本地文档”直接交给来源 Skill，是事故的直接规则来源。
- 当前 `source commit` 把来源处理和新闪念创建绑定为一个事务。
- 当前所有正式写入都会检查全部来源快照并重建索引。
- 当前七个 Skills 中，capture 与 literature 需要实质修改；form-permanent、review-permanent 和 lint 需要收紧交界；review-process 与 connect-cards 保持不变。
- 捕获会话应当支持多轮交流和多张闪念，用户需要在最终确认前反复修正候选清单。
- 临时认知现场只负责防丢，不应默认进入 Git 或永久保留。

## 实施期间新增发现

- 正式 Git 事务与运行时维护队列不能靠调用顺序假设原子性：维护任务 ID 先写入正式事务，事务重放负责修复提交后崩溃留下的会话转移缺口。
- 只把幂等记录保存在会话内部无法覆盖 start 重放和 discard 后重放；运行时需要 Git 忽略的全局事务回执，同时保留会话内记录用于崩溃恢复。
- “新闪念优先”不能只写在 Skill 中；`capture start` 必须在安全原子边界自动把 processing 任务转为 `maintenance_paused`。
- 后台状态不能自由改写；状态迁移白名单和三次重试上限由 CLI 内核执行，Skill 只负责判断和调用。
- 后台来源处理仍由 Agent 按 Skill 消费持久队列，而不是常驻 daemon；自动性指用户确认后无需继续等待或指挥，恢复性来自任务文件、指纹门禁和确定性 CLI。
