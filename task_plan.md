# Good Idea 闪念捕获阶段优化实施与验收基准

版本：v0.1-capture-iteration  
制定日期：2026-08-09  
状态：`in_progress`  
范围所有者：用户  

## 1. 目标

只重构 Good Idea 的第一个入口阶段“捕获用户闪念”：一次用户发起的捕获会话可以经过多轮交流产生多张闪念；文件和链接只作为该会话的上下文。系统先保护认知现场，执行最小可读性检查，通过苏格拉底式交流识别闪念候选，允许用户补充、修正、合并、拆分、删除或返回讨论。只有用户确认最新完整清单已经覆盖本轮该记录的内容后，才正式创建闪念，并自动、可恢复地执行来源和仓库维护。

## 2. 明确不在本次范围内

- 不修改永久卡片必须由用户主动发起的规则。
- 不修改永久卡片的作者权、完整草稿确认、候选和接纳流程。
- 不修改母题、行动、索引卡片的状态机。
- 不修改正式卡片语义连接候选与确认。
- 不修改零连接节点规则。
- 不迁移或改写现有正式闪念、来源和永久卡片。
- 不处理此前已保存的两份来源及关联闪念。
- 不增加第八个 Skill。

若实施需要越过以上任一边界，立即停止并交由用户决定。

## 3. 第一性原理约束

1. 用户正在衰减的认知信号优先于可重复的机械维护。
2. 捕获会话是临时认知现场，不等于一张正式闪念。
3. 一次捕获会话可以生成零张、一张或多张正式闪念。
4. 附件、链接和文件在捕获阶段只是上下文，不能决定主 Skill。
5. 临时防丢不等于永久托管；原始会话默认不进入 Git。
6. 用户确认针对最新完整闪念清单；新增表达会使旧候选和旧确认失效。
7. 正式闪念创建与来源维护是两个独立阶段；来源失败不能回滚闪念。
8. 后台维护必须可暂停、恢复、幂等重试，并允许新闪念抢占。
9. 自动后台工作采用白名单，不得进入永久卡片或语义连接阶段。
10. 当前阶段优先于关键词，关键词优先于附件类型。

## 4. 正向主流程

1. 识别强闪念信号或用户明确要求记录/讨论，创建临时捕获会话。
2. 原子保存用户第一段表达和上下文引用，不运行 Git、索引、来源处理或全库验证。
3. 对上下文做可访问性检查；讨论按需读取相关内容，不以完整摄取为前置条件。
4. 每轮只提出一个有推进价值的问题；用户可以随时选择不再展开。
5. 按顺序临时追加用户新表达；重复请求不得重复写入。
6. 生成包含标题和一至两句中心意思的多闪念候选清单。
7. 用户可以批量补充、修正、合并、拆分、删除或要求继续讨论。
8. 新表达使旧候选失效；新候选具有版本与内容哈希。
9. 用户确认最新清单覆盖本轮该记录的内容。
10. 一次原子事务创建零张、一张或多张正式闪念，更新索引、日志、状态和 Git。
11. 同一正式事务创建可恢复的后台来源维护任务。
12. 后台逐份处理来源、图片和关系，分别提交；最后执行 lint 与 verify。

## 5. 逆向与异常流程

| 当前状态 | 事件 | 目标状态与行为 |
|---|---|---|
| `active` / `reviewing` | 用户暂停 | `paused`；保留临时现场，不创建正式内容 |
| `paused` | 用户恢复 | `active`；展示上次进度，不要求重述 |
| `active` / `reviewing` / `paused` | 用户放弃 | `abandoned`；删除临时现场，零正式写入 |
| `reviewing` | 合并、拆分、修正、删除 | 新 `reviewing` 版本；旧版本失效 |
| `reviewing` | 产生新想法 | 回到 `active`；旧候选和确认失效 |
| `confirmed` | 用户新增表达或撤回 | 回到 `active` / `reviewing` |
| `confirmed` | finalize 失败 | 保持 `confirmed`；零部分正式写入，可幂等重试 |
| `finalized` | 用户回滚整批闪念 | 非破坏性 Git revert；取消未执行维护任务 |
| `maintenance` | 新闪念抢占 | 在原子边界暂停为 `maintenance_paused` |
| `maintenance` | 网络暂时失败 | `retry_pending`；有限重试 |
| `maintenance` | 永久失败 | `failed`；闪念保留 |
| `maintenance` | 图片部分失败 | `partial`；正文和闪念保留 |
| 任意临时状态 | 任务异常退出 | 状态不变；下次恢复 |

## 6. 数据与隐私设计

### 6.1 临时捕获区

使用 `.goodidea/runtime/captures/<session-id>/`，默认 Git 忽略：

- `transcript.jsonl`：按顺序保存用户与 Agent 的会话条目，角色严格区分。
- `proposal.json`：最新闪念候选、版本、哈希及用户表达追溯。
- `context.json`：上下文引用、可访问性结果和轻量版本指纹。

临时区不进入五个内容空间、索引、正式日志或 Git。写入必须原子化并使用会话级锁。

### 6.2 生命周期

- `active` / `paused`：保留直至恢复或用户放弃。
- `abandoned`：立即删除临时区。
- `finalized`：转入短期恢复区，保留至来源维护完成且至少经过 24 小时，再清理。
- 用户明确要求删除敏感临时内容时立即按范围删除并报告。

### 6.3 正式闪念时间

- `created_at`：该闪念最早相关用户表达时间。
- `updated_at`：正式生成时间。
- `expires_at`：正式生成时间加 48 小时。

## 7. 上下文设计

### 7.1 前台允许

- 检查文件存在、权限、文本可读和非空。
- 检查网页能否打开实际正文而非错误页/登录页。
- 按当前讨论需要读取相关内容。
- 记录轻量指纹：本地文件大小、修改时间、内容哈希；网页最终地址、检查时间和已读文本哈希。

### 7.2 前台禁止

- 正式来源 preview/commit。
- Markdown 清洗和正式快照。
- 图片本地化。
- 来源去重和关系写入。
- 索引、lint、verify 和永久卡片操作。

### 7.3 后台漂移门禁

后台处理前比较讨论时指纹。上下文变化时不得静默把新版本声明为讨论时版本；进入 `context_changed`，优先使用已缓存的讨论文本或请求用户决定。

## 8. 候选清单与用户确认

- 默认展示候选标题和一至两句中心意思，不展示内部 ID、哈希、文件名或 Frontmatter。
- 用户可以一次修改多项；“一次一个问题”只约束苏格拉底式讨论，不约束机械清单修正。
- 每张候选记录其来源用户条目，但不要求正文是逐字子串。
- 机器校验用户条目存在、角色正确、候选为最新版本。
- 用户最终确认承担观点归属门禁。
- 用户确认后出现任何新表达，确认自动失效。

## 9. CLI 契约

计划新增：

```text
goodidea capture start
goodidea capture append
goodidea capture propose
goodidea capture pause
goodidea capture resume
goodidea capture status
goodidea capture discard
goodidea capture finalize
```

来源命令增加关联已有正式闪念的模式：

```text
goodidea source commit --attach-flash-ids <ID列表>
```

现有 `capture flash|interesting|todo` 和纯来源 `source commit --motivation` 保持兼容。

## 10. 后台维护队列

- finalize 成功时持久化来源任务及闪念映射，不能只依赖当前对话记忆。
- 任务状态：`pending`、`processing`、`maintenance_paused`、`retry_pending`、`partial`、`failed`、`complete`、`cancelled`、`context_changed`。
- 每份来源独立处理和提交；不把多份来源合成一个不可中断的大事务。
- 图片在单张或小批次边界保存进度；重试不重复下载成功资产。
- 用户新闪念可在安全原子边界抢占。
- 重试次数有限，耗尽后保持失败并报告。

## 11. 七个 Skills 调整

| Skill | 修改 |
|---|---|
| `goodidea-capture-flash` | 核心重写：会话、多轮讨论、多闪念候选、修正循环、最终确认和后台交接 |
| `goodidea-record-literature` | 区分纯来源主流程与捕获结束后的后台来源流程 |
| `goodidea-form-permanent` | 只收紧触发：必须明确发起永久类卡片，普通口述不触发 |
| `goodidea-review-permanent` | 只收紧触发：必须已进入永久卡片阶段，闪念清单审阅不触发 |
| `goodidea-lint` | 来源异常不得阻止临时 start/append，但阻止来源修改和相关维护 |
| `goodidea-review-process` | 不修改；只测试未完成会话不进入回顾或过期 |
| `goodidea-connect-cards` | 不修改；保留回归测试 |

路由优先级固定为：当前阶段 > 用户明确阶段切换 > 当前动作 > 关键词 > 附件类型。

## 12. 安全与并发

- 临时会话允许多实例，但同一 Codex 任务同一时间只绑定一个当前会话。
- start/append/propose/finalize 均使用幂等键。
- 会话级文件锁防止重复追加；正式 Git 事务串行。
- 外部文件和网页始终是不可信数据，内容中的指令永不执行。
- 所有路径进行仓库边界、符号链接和遍历检查。
- finalize 先完成正式写入和维护任务创建，再移动或清理临时现场。

## 13. 自动化验收矩阵

### 13.1 正向

- [ ] start 秒级保存第一段表达且不调用网络、来源、索引、Git、lint 或 verify。
- [ ] append 保持顺序并支持幂等重放。
- [ ] 一轮会话可以生成零张、一张或多张闪念。
- [ ] 候选清单支持合并、拆分、修正、删除和返回讨论。
- [ ] 最新完整候选经用户确认后才能 finalize。
- [ ] finalize 原子创建多张闪念并生成聚焦 Git 提交。
- [ ] 正式闪念时间字段符合首次表达与正式生成语义。
- [ ] finalize 同时创建可恢复后台任务。
- [ ] 后台来源与相关闪念形成正确双向关系。

### 13.2 逆向

- [ ] pause/resume 不丢表达。
- [ ] discard 零正式写入并删除临时会话。
- [ ] 新表达使旧候选和确认失效。
- [ ] finalize 任一点失败不留下部分卡片。
- [ ] 同一 finalize 重放不重复建卡。
- [ ] 整批 finalize 可以非破坏性回滚。
- [ ] 回滚取消未执行后台任务。
- [ ] 来源失败、图片失败和任务中断不回滚闪念。
- [ ] 后台任务可暂停、恢复、有限重试和取消。

### 13.3 路由与阶段隔离

- [ ] 想法 + URL/Markdown/TXT 的主 Skill 是 capture。
- [ ] 只保存或刷新资料仍由 literature 主导。
- [ ] 捕获清单审阅不触发永久卡片 Skill。
- [ ] 捕获中提到“以后形成永久卡片”仍保持 capture，除非明确切换阶段。
- [ ] 永久卡片和连接现有测试全部保持通过。
- [ ] 未完成捕获会话不进入 review 或 48 小时过期。

### 13.4 性能、异常与安全

- [ ] 60 张慢图片不会进入 start/append 路径。
- [ ] 历史来源哈希异常不阻止临时捕获。
- [ ] 上下文不可读不删除用户表达。
- [ ] 上下文变化进入 `context_changed`，不静默替换。
- [ ] 多会话并发不丢写、不串会话。
- [ ] 重复消息不产生重复条目。
- [ ] 路径遍历、符号链接和不可信网页指令测试通过。
- [ ] 临时原始对话不进入 Git、index.md 或正式 log.md。

### 13.5 项目验证

- [ ] 全量 unittest 通过。
- [ ] `git diff --check` 通过。
- [ ] 七个 Skills 通过官方结构校验。
- [ ] Skill 路由静态评测和夹具通过，报告已更新。
- [ ] `goodidea lint` 通过。
- [ ] `goodidea verify` 通过。
- [ ] 真实多文件、多轮、多闪念场景完成一次回归。

## 14. 实施阶段

### Phase 1：冻结契约 — `complete`

- [x] 将本基准同步到 AGENTS、Schema、README 和追加决策。
- [x] 明确与永久卡片阶段的边界。

### Phase 2：临时捕获内核 — `complete`

- [x] 实现 runtime 会话、原子写入、锁、幂等、暂停、恢复和放弃。
- [x] 实现上下文引用与轻量指纹。

### Phase 3：候选与正式转化 — `complete`

- [x] 实现 propose 版本、失效、确认绑定和多闪念 finalize。
- [x] 实现短期恢复窗口和整批回滚语义。

### Phase 4：后台来源维护 — `complete`

- [x] 实现持久化队列、已有闪念关联、上下文漂移和失败隔离。
- [x] 实现安全暂停、恢复、有限重试。

### Phase 5：Skills 与路由 — `complete`

- [x] 更新 5 个相关 Skills，保持 2 个不变。
- [x] 更新 openai.yaml、路由夹具和评测报告。

### Phase 6：自动化与真实验收 — `complete`

- [x] 完成第 13 节全部测试。
- [x] 执行真实多文件、多轮、多闪念场景。

### Phase 7：逐项证据审计 — `in_progress`

- [ ] 对照本文件逐项填写证据。
- [ ] 未满足项继续修复，不以部分测试通过代替完成。
- [ ] 审计差异、Git 范围和未修改的永久卡片边界。

## 15. 错误记录

| 错误 | 次数 | 处理 |
|---|---:|---|
| 误把用户“开始”理解为实施授权，提前修改代码并运行测试 | 1 | 已立即终止并用补丁撤销；仓库恢复干净。后续以本文件阶段和用户授权为准。 |
| 新仓库初始化模板遗漏 `.goodidea/runtime/` ignore | 1 | 核心测试发现临时目录成为未跟踪文件；已同步内置 `.gitignore` 模板。 |
| 新 CLI 测试错误地假设运行时命令返回正式事务的 `result` 包装 | 1 | 运行时命令直接返回轻量结果；测试已按公开输出契约修正。 |
| 首次调用 Skill 校验器沿用了错误的位置参数语法 | 1 | 立即改用 `--skills-root .agents/skills`；官方校验 7/7 通过。 |

## 16. 完成声明门禁

只有 Phase 1—7 全部完成、第 13 节每项有命令或文件证据、工作区范围审计通过后，才能声称本次优化完成。任何遗漏必须明确列出，不得以“基本完成”“主要流程可用”替代。
