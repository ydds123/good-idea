# Skill 层

当前没有正式 Skill，也不预设 Skill 的数量、名称、目录或文件结构。

Agent 直接读取规则层即可开展试运行。只有真实使用反复暴露稳定触发、路由冲突或明显上下文负担时，才讨论是否创建 Skill。

未来 Skill 即使出现，也只能加载和编排 `rules/` 已有规则，不能重新定义对象、状态、权责或协议。删除、合并或更换 Skill 后，规则与数据仍必须成立。
