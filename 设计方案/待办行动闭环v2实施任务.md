# 任务：待办行动闭环 v2——摄入澄清 + 48h 过期状态机

> 文档性质：任务交接说明，不是生效规则。由用户指定其他 Agent 执行；执行 Agent 不得自行放宽 AGENTS.md、schema.md 或用户作者权边界。
>
> 交接日期：2026-08-15

## 一句话任务

把待办空间从"只进不出"升级为"行动闭环"：**输入侧**加澄清式捕获（模糊才问、答案进卡片"为什么做"区、估价引用），**状态侧**加 48h 行动窗口（新建=未开始，超 48h 未开始→已过期，移出当前行动清单等待用户裁决），**阅读层**同步（行动清单只显示未开始+进行中）。

## 背景与现状

### 已有机制（2026-08-15 已上线，不要重复造）

1. **估价机制**：`capture valuate` 为进行中待办写入七个标签（需求类型/高阶目标/等效性/多效性/成功概率/距离/具体性）+ priority（期望×价值×距离排序：概率×多效性×距离主键、等效性 tie-break、未估价排最后）。理由必填（`--rationale`，机制拒绝黑箱），渲染成卡片"估价依据"区（对象|内容|说明 三列 Markdown 表格，含确定性得分与优先级）。标签枚举受控（need_type 固定 3 种、goal_id 固定 4 个 GOAL_SPECS），数据库视图下拉单选。
2. **状态归档**：待办状态子目录（`已完成/`、`已取消/`），`capture transition` 流转归档 + 全库 wikilink 重写；`capture sync` 扫描待办（根/已完成/已取消）与闪念（根/已处理）双向归位。
3. **估价 cron**：job `2a39a27def65`，0/8/16 点跑，自动估价新待办；估价前做状态检查（事实已完成/失效的先 transition 归档）。
4. **方法论**：闪念卡《目标规划方法论：目标层级金字塔、目标系统理论（等效性/多效性）与目标特征》（`闪念空间/2026-08-14-...md`）——估价排序、六特征标签的单一事实源。

### 缺口

1. **摄入侧不问为什么**：`capture todo` 直接保存，不追问动机。当前 9 条待办里多条只有标题一句话（如"阅读《Raycast...》"），无"为什么做"，估价靠猜。
2. **无未开始/过期概念**：待办只有进行中/已完成/已取消。事实没开始做的（念头起来 48h 没动）仍躺在行动清单里，不产生行动。
3. **昨晚方案 A 搁置**：`待办空间/2026-08-14-待办行动闭环方案...` 里存有 2026-08-14 拍板的方案 A（未开始档 + 必填原因 + 48h 预警），本次实施吸收它并升级。

## 用户确认的设计（2026-08-15 拍板）

### A. 摄入侧：澄清式捕获

- **触发原则：B（模糊才问）**——用户口述内容已含动机的，直接存不追问；只有一句话/意图不明/无动机信息的才追问。全问会制造流程摩擦。
- **问题集（一次一个，对话式，不是表单）**：
  1. 为什么做这件事？（动机/价值 → 喂需求类型+高阶目标）
  2. 不做会怎样？（损失/机会成本 → 喂值不值得）
  3. 做完是什么样？（期望产出 → 喂具体性）
  4. 打算什么时候做？（时间窗口 → 喂距离+投入规模）
- **落点**：卡片正文顶部新增"## 为什么做"区（用户确认过的原话记录，属于用户认知正文，非 CLI 机械区）。`capture todo` 支持可选 `--reason` 写入该区。
- **估价衔接**：估价 cron prompt 要求先读"为什么做"区；valuate 的 rationale 引用它作为判断依据，不再只靠正文猜。

### B. 状态机：48h 行动窗口

```
新建待办 → 未开始 ──48h 内开始──→ 进行中 ──→ 已完成
              │                      ↑
              │ 超过 48h 未开始       │ 重新承诺
              ↓                      │
           已过期 ──裁决──→ 干掉 → 已取消
```

- **未开始**：新建待办默认状态（替代现在的默认"进行中"）。
- **进行中**：用户开始做（阅读层数据库视图改状态 / CLI transition）→ 退出 48h 窗口。
- **48h 过期**：估价 cron（0/8/16 点）顺带扫描——`未开始` 且创建超 48h → **已过期**，物理移入 `待办空间/已过期/` 子目录（与已完成/已取消同构，含全库 wikilink 重写）。已过期从当前行动清单消失。
- **裁决**：已过期待办由用户二选一——重新承诺（transition 回 未开始/进行中，移回根目录，重新计时）或干掉（transition 到 已取消）。
- **推送保持不做**（用户 2026-08-15 明确拍板：推送渠道先不用）。过期通过数据库视图呈现。

### C. 阅读层（数据库视图）

- `Good idea 数据库/待办数据库.md`：行动清单 = 未开始 + 进行中（过滤已过期/已完成/已取消），按 priority 排序；状态列 statusOptions 增加 未开始/已过期 语义色（未开始灰/进行中绿/已过期红）。已过期不在主视图出现（或单独视图）。
- 用户开始做 = 在数据库视图把状态列改为"进行中"，CLI `capture sync` 收口（沿用现有阅读层手动修改模式）。

## 需要改动的位置

| 文件 | 改动 |
|---|---|
| `src/goodidea/contracts.py` | TODO 状态枚举加 未开始/已过期；TODO_STATUS_DIRS 增加 `expired: 待办空间/已过期`；根目录语义改为 未开始+进行中（open 集合） |
| `src/goodidea/service.py` | capture todo 默认未开始 + `--reason` 写"为什么做"区；transition 支持未开始/已过期流转；valuate 增加过期扫描（未开始超 48h → 已过期 + 归档 + 重排）；valuate 只估价 未开始+进行中 |
| `src/goodidea/notes.py` | 如需要：目录扫描展开 已过期 子目录 |
| `src/goodidea/cli.py` | capture todo `--reason` 参数；transition 状态枚举提示更新 |
| `tests/` | 状态机流转、过期扫描、摄入澄清写区、估价只算行动集、归档联动测试 |
| `Good idea 数据库/待办数据库.md` | 状态列 statusOptions、行动清单过滤 |
| cron `2a39a27def65` prompt | 估价前读"为什么做"区；顺带过期扫描说明 |
| `.agents/skills/goodidea-capture-flash/SKILL.md` | 摄入澄清对话流程（模糊才问 + 四问） |
| `schema.md`、`README.md` | 状态机、摄入澄清、48h 过期说明 |
| `src/goodidea/repository.py` | 如需要：find_note 展开 已过期 子目录 |

## 存量迁移（执行 Agent 开工前必须先向用户确认）

1. **计时方式**：9 条存量"进行中"迁到"未开始"后，48h 从**迁移日**重新计时（给存量一次重新承诺机会；用户 2026-08-15 倾向此方案）还是从 created_at 严格算（存量几乎全过期，直接进入裁决）？——先确认再迁移。
2. **顺带裁决**：迁移时请用户裁决明显不想做的（如"看板统计"用户自注"不一定要做"），可 transition 已取消。
3. **"开始做"触发**：阅读层改状态 + `capture sync` 收口（沿用现有模式，不加新交互）——确认用户无异议。

## 参考材料（按权威顺序）

| 材料 | 作用 |
|---|---|
| `AGENTS.md` | 最高权责：规则 1/7（空间/事务）、任务路由、Agent 操作顺序、工作完成标准 |
| `schema.md` | 机器契约：待办字段、状态机、事务、估价（2026-08-15 段） |
| `src/goodidea/`（contracts/service/cli/notes/repository） | 现有实现：状态目录、transition/sync/valuate、估价排序 |
| `待办空间/2026-08-14-待办行动闭环方案（未开始档+必填原因+48h预警）实施.md` | 昨晚方案 A 全文（本次实施吸收的原始来源） |
| `闪念空间/2026-08-14-目标规划方法论....md` | 方法论单一事实源（估价六特征、期望×价值×距离） |
| `Good idea 数据库/待办数据库.md` | 阅读层视图（note-database，行动清单） |
| `.agents/skills/goodidea-capture-flash/SKILL.md` | 摄入对话流程（本次加澄清节） |
| `~/.hermes/skills/goodidea/goodidea-development/SKILL.md` | 开发态技能（含估价机制说明，需同步） |

## 约束（不可违反）

1. **用户作者权边界不变**：摄入澄清的答案必须用户确认后才写入卡片；"值不值得/干不干掉"的裁决权在用户，Agent 只呈现选项（已过期裁决二选一），不得替用户决定。
2. **不得放宽 AGENTS.md / schema.md**：改 schema、状态机、契约前先向用户汇报具体改动方案，确认后再落地。
3. **修正融入正文，单一事实源**：状态机/澄清流程直接写入 schema/README/Skill 本体，不得以补丁式说明保留新旧两套说法。
4. **聚焦提交**：每个提交只含本任务相关文件，不带入用户已有未提交改动（Obsidian 运行时状态 `.obsidian/*`、数据库视图用户微调一律不碰）；目标文件脏改动时停止并说明。
5. **Skill 校验**：修改项目级 Skill 后运行 `uv run python scripts/validate-skills.py`；若修改 description 或任务路由还需 `uv run python scripts/evaluate-skill-routing.py`。
6. **先出方案，经用户确认后再改**：涉及协作层规则（capture-flash 对话流程）与机器契约（状态机），执行 Agent 应先给出具体改动方案，用户确认后再落地写入。
7. **推送保持不做**：本任务不引入任何推送渠道（飞书等），48h 过期通过数据库视图呈现。
8. **枚举受控**：新状态/标签枚举值写中文、进 contracts 受控清单；数据库视图 statusOptions 同步。

## 验收标准

- 新建待办默认 `status: 未开始`；摄入内容模糊时 Agent 触发澄清追问（四问），用户确认的答案出现在卡片"## 为什么做"区
- 估价 cron 扫描：`未开始` 超 48h → `已过期`，物理移入 `待办空间/已过期/`，全库 wikilink 重写，从行动清单消失
- 已过期可裁决：transition 回 未开始/进行中（移回根目录）或 已取消
- `capture valuate` 只估价 未开始+进行中；已过期/已完成/已取消不参与估价排序
- 待办数据库行动清单只显示 未开始+进行中（按 priority），状态列含 未开始/已过期 选项
- 存量 9 条迁移完成（用户确认计时方式后），用户顺带裁决的条目已取消
- 全量测试通过（`uv run python -m unittest discover -s tests`）、`goodidea lint` 干净、`goodidea verify` 通过
- schema.md / README.md / 项目 Skills / Hermes 侧 goodidea-development skill 已同步

## 执行边界

- 本任务含机器契约改动（状态机、CLI）与协作层改动（摄入澄清流程），均可实施；但**开工前先就"存量迁移"两项向用户确认**
- 不创建正式卡片、不修改现有卡片正文（除存量迁移的状态流转）；每次写操作走 `goodidea` CLI 事务
- AGENTS.md 若需同步（任务路由/规则），因写入审批门禁（agent-client 无审批通道），列出建议改动由用户手动完成，不由 Agent 强改
