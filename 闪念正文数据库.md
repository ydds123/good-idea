# 闪念正文数据库

> 实时查询闪念空间卡片正文（依赖 Dataview 插件）。
> 与 `闪念空间/闪念数据库.base` 分工：Base 管元数据（状态/时间/来源数），本文件管正文。
> 本文件放在 vault 根目录而非闪念空间——它是阅读层查询，不是内容卡片。

## 摘要视图

```dataview
TABLE WITHOUT ID
  file.link AS "卡片",
  status AS "状态",
  substring(file.content, 0, 150) AS "内容摘要",
  created_at AS "创建时间"
FROM "闪念空间"
WHERE type = "闪念"
SORT created_at DESC
```

## 全文流

```dataview
LIST
FROM "闪念空间"
WHERE type = "闪念"
SORT created_at DESC
```
