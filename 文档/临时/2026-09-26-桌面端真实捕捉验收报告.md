# 桌面端真实捕捉验收报告

- 验收日期：2026-09-26（验收执行与报告落盘：22:48–23:26，本地时间）
- 验收对象：Hermes 桌面端 + 回合后捕捉适配器，真实捕捉一次完整生命周期（进入 → 多轮自动转录 → stop 收尾 → 自动提交）
- 结论摘要：**通过**——自动转录 9 轮逐条对位；stop 回合落盘、session 注销、自动提交三项均在真实桌面捕捉中完成并核实

## 环境

- Hermes 版本：Hermes Agent v0.21.4（2026.9.21），upstream `8afaab37`；安装方式 git；安装目录 `/Users/apple/.hermes/hermes-agent`
- 桌面端启动方式：桌面应用拉起的后端进程。
  - 应用进程：`/Users/apple/.hermes/backups/desktop/Hermes-pre-alignment-20260827-2018.app/Contents/MacOS/Hermes`（启动于 2026-09-26 22:46:35）
  - 后端进程：`~/.hermes/hermes-agent/venv/bin/python -m hermes_cli.main serve --host 127.0.0.1 --port 0`（启动于 22:46:36；监听 `127.0.0.1:51178`）
  - 说明：本次重启（22:46）之后桌面会话的自动转录才生效；本验收即重启后的首次真实桌面捕捉
- 仓库路径：`/Users/apple/Documents/Goodidea`
- 当前分支：`v0.16`
- 适配器配置路径：`~/.hermes/config.yaml` → `hooks.post_llm_call`；授权记录 `~/.hermes/shell-hooks-allowlist.json`
- `hermes hooks list` 结果：

```text
Configured shell hooks (1 total):

  [post_llm_call]
    - python3 /Users/apple/Documents/Goodidea/工具/hooks/hermes_post_turn.py (timeout=15s, ✓ allowed)
      approved_at: 2026-09-26T14:04:25.501861Z
```

## 捕捉过程

- session_id：`20260926_224850_786596`
- 记录标题：认知卸载、元认知与人-AI协作（`数据/记录/闪念/原始思考记录/2026-09-26-认知卸载、元认知与人-AI协作.md`）
- 开始时间：2026-09-26 22:48:50（触发消息）；22:50:11 登记（`capture.py start --resume`，续接同日既有记录）
- 进行了几轮对话：9 轮（第 1–8 轮为讨论；第 9 轮为收口/停止轮）
- 是否讨论候选：是——第 6 轮回复给出拆分方案（两份闪念候选＋观察组去向）；未被认领，未转正（零产出收口）
- stop 发生在哪一轮：第 9 轮（记录时间戳 23:22；`capture.py stop` 后状态转 `closing`，同一回合末由适配器完成落盘＋注销＋提交）
- 记录范围说明：同一记录内另含同日更早的手工阶段 11 轮（15:19–17:02，不属本次适配器验收范围）；本验收覆盖其中适配器写入的 9 轮（40 个消息块中的后 18 个）

## 自动转录验收

| 项 | 结果 | 证据 |
|---|---|---|
| 用户消息是否自动写入 | 是（9/9） | 记录中「松海」消息块 9 条，与 9 轮逐条对位 |
| Agent 最终回复是否自动写入 | 是（9/9） | 记录中「Agent」消息块 9 条；含 stop 轮收尾消息 |
| 是否包含工具调用 | 否 | 适配器 9 轮范围抽查 0 命中；记录全文唯一命中为更早手工阶段 Agent 消息中自述的复核命令 |
| 是否包含思考过程 | 否 | 均为可见文本；抽样第 1 轮回复块 2,066 字符（含块头），与回合日志 `response_len=2036` 一致 |
| 是否包含系统内容 | 否 | 抽查 `turn_id`、`conversation_history`、`hook_event_name` 等内部字段：0 命中 |
| 消息顺序是否正确 | 是 | 9 轮按时间升序，每轮「松海→Agent」成对，无交叉 |
| 时间和说话人是否正确 | 说话人＝是；时间＝回合结束时刻（同一回合两条共用一个时间戳），与实际发出偏差 0–5 分钟、等于该回合耗时 | 逐轮对位，见下表 |
| 重复事件是否重复写入 | 否 | 40 个消息块无重复；唯一文字二次出现为后轮消息中的引用；同一载荷重放去重已在更早阶段验收覆盖 |
| stop 后的消息是否继续写入 | 否 | 注销已核实（`status` → `active:false`；`capture-sessions.json` → `{}`）；合成同构事件实测：退出码 0、记录哈希不变、无标记写入；第 10 轮自然消息未出现在记录中 |

逐轮时间（实际发出 → 记录时间戳）：22:48:50→22:53、22:55:21→22:57、22:58:15→22:59、23:00:07→23:00、23:01:01→23:01、23:03:01→23:03、23:06:12→23:08、23:09:27→23:13、23:18:16→23:22。

## 自动提交验收

- stop 所在回合最后消息是否落盘：是（记录末尾＝第 9 轮「松海」＋「Agent」两条，时间戳 23:22；其后无内容）
- session 是否注销：是（`capture.py status --session …` → `{"active": false}`；`capture-sessions.json` 为空对象 `{}`）
- 是否自动执行 Git 提交：是（适配器在落盘并确认注销后调用 `工具/commit.py`，无需人工触发）
- commit ID：`d2074d48d7a18a8a75861b8ea446f964f079be0b`
- 提交是否只包含目标记录：是（`1 file changed, 375 insertions(+)`，仅记录文件；`.obsidian/workspace.json` 等未入）
- 记录收尾全文哈希（sha256）：`acf29ae80a38f1cf801fdb56aafce011f48631b1c446857383f66723662abd94`

## 异常与日志

- `.goodidea/tools.log` 是否有错误：否（全文仅 1 行，为 22:02:43 更早一次测试会话的 start 拒绝记录，早于本次验收）
- 是否出现适配器错误：否
- 是否出现漏写、重复、乱序：无（9 轮 18 条逐条对位；无重复块；顺序正确）
- 备注：Hermes 会话库（`~/.hermes/state.db`）中本会话消息存在压缩前后两份副本（Hermes 内部存储行为，不影响捕捉记录侧唯一性）
- 脱敏错误摘要：无

## 结论

- 是否通过桌面端真实捕捉验收：**通过**
- 尚未验证的事项：
  1. stop 后自然消息的长期不写入：机制已实测（注销旁路、合成事件、第 10 轮样本），可继续对话抽样复核
  2. 中断回合（SIGINT）的可见消息不会被转录（阶段 0 已知缺口，非本次缺陷）
  3. provider 彻底失败且无回落时的回合行为（阶段 0 遗留项）
  4. 「固定加载清单」优化收益（本机未实施，无数据）
- 建议后续动作：
  1. 在本会话继续自然对话一两条，复核记录确实停止在 stop 轮
  2. 第 6 轮候选拆分方案未认领；若要接着弄，另起流程（不依赖捕捉窗口）
  3. 观察组（适配器落盘、耗时、时间戳、原句多次带入）归位到 `验证/试运行记录/`
  4. 桌面端重启或升级后，重跑一次同构验收（本报告可作模板）

## 证据

（以下为报告落盘前实测；本报告自身的提交与推送状态见交付回执。）

```text
$ git status --short --branch
## v0.16...origin/v0.16 [ahead 1]
 M .obsidian/workspace.json

$ git log --oneline -5
d2074d4 capture: 认知卸载、元认知与人-AI协作
a81873b chore: 移除 goodidea-ops（仓库操作手册）
370dd91 docs: 收敛 goodidea-ops 的过时说法与规则重复段
d1fcc5c chore: 把 goodidea-ops / goodidea-source-sync 迁入仓库 .agents/skills
7f2f103 chore: 移除旧占位目录 数据/记录/原始思考会话（术语统一收尾）
```

```text
$ hermes hooks list
Configured shell hooks (1 total):

  [post_llm_call]
    - python3 /Users/apple/Documents/Goodidea/工具/hooks/hermes_post_turn.py (timeout=15s, ✓ allowed)
      approved_at: 2026-09-26T14:04:25.501861Z
```

```text
$ git show --stat d2074d4
commit d2074d48d7a18a8a75861b8ea446f964f079be0b
Author: <脱敏>
Date:   Sat Sep 26 23:22:48 2026 +0800

    capture: 认知卸载、元认知与人-AI协作

 .../2026-09-26-认知卸载、元认知与人-AI协作.md      | 375 +++++++++++++++++++++
 1 file changed, 375 insertions(+)
```

```text
$ shasum -a 256 数据/记录/闪念/原始思考记录/2026-09-26-认知卸载、元认知与人-AI协作.md
acf29ae80a38f1cf801fdb56aafce011f48631b1c446857383f66723662abd94
```
