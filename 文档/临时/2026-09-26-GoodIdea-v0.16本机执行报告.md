# Good Idea v0.16 本机执行报告

## 1. 执行环境

- 仓库路径：`/Users/apple/Documents/Goodidea`
- 当前分支：`v0.16`（`git rev-parse --abbrev-ref HEAD` = v0.16）
- 基础提交：`fb35fb3`（工件单进入时的 HEAD：`docs: 增加本机 Hermes 阶段执行任务说明`）
- 最终提交：`9436de0`（`docs: 补充真实性能测量结果`，为第 5 个提交的 amend 版）
- Hermes 版本或环境说明：Hermes Agent `v0.21.4 (2026.9.21)`，upstream `068db016`，安装方式 git，安装目录 `/Users/apple/.hermes/hermes-agent`；配置 `~/.hermes/config.yaml`；日志 `~/.hermes/logs/agent.log`（29,147 行）；模型 `deepseek-flash`（deepseek 官端）；代理 `127.0.0.1:7897`（仅 gh/git 走）
- 是否修改 main：否
- 是否 push：否（截至本报告落盘时：`v0.16...origin/v0.16 [ahead 5]`）

## 2. 阶段 0：Hermes 事件探针

- 探针文件路径：`工具/hooks/hermes_event_probe.py`（104 行）；日志 `.goodidea/probe.log`（23.6 KB）
- Hermes 钩子配置位置：`~/.hermes/config.yaml` 的 `hooks.post_llm_call`；授权记录 `~/.hermes/shell-hooks-allowlist.json`（逐 (事件, 命令) 授权，用 `hermes chat --accept-hooks` 记录，`approved_at 2026-09-26T14:04:25Z`）
- 是否取得最终可见 Agent 回复：是（`assistant_response`；含 2 次工具调用的回合验证：只含最终那段文字，长度 45，不含工具调用、思考、系统内容）
- 是否取得用户可见消息：是（`user_message`）
- session_id 是否稳定：稳定；`--resume` 同一会话两回合为同一 id，形如 `20260926_215613_6f1774`
- turn_id 是否稳定：稳定；形如 `<session>:<session>:<8hex>`，长度 54，同一回合的 `post_llm_call` 与 `on_session_end` 同值
- 是否出现重复触发：否（单钩子配置下每回合每事件恰好 1 次，8 个回合全部如此；同一事件配多个钩子会各触发一次，属配置条数）
- 是否出现乱序：否（同回合 `post_llm_call` 先于 `on_session_end`，跨回合按时间序）
- 回复失败后是否触发：分两类——① 模型 400 被凭据回落接管，回合最终成功并正常触发（payload `model=gpt-5.6-sol-900k`）；② 回合被中断（SIGINT）时 `post_llm_call` **不触发**，只有 `on_session_end` 触发且带 `completed=False / interrupted=True / turn_exit_reason=interrupted_during_api_call`、无正文
- 钩子类型：同步（回合最终化等待钩子返回）
- 耗时样本数：13（不含故意睡眠样本）
- p50：0.4 ms
- p95：0.5 ms
- 最大耗时：0.5 ms（Hermes 侧 `hermes hooks test` 计时 0.048 s；钩子 exit 3 + 非 JSON 输出只写 WARNING，回合 5.1 s 照常完成）
- stop 所在回合是否可靠到达：结构上可靠（回合最终化时携带该回合 user_message + 最终回复；带 2 次工具调用的回合已验证），真实捕捉流程中的验证放到阶段 3b 验收（已通过，见第 3 节）
- 事件样例摘要（脱敏）：

```json
{
  "session_id": "<len=22>",
  "turn_id": "<len=54>",
  "user_text_length": 26,
  "assistant_text_length": 14,
  "conversation_history_len": 2,
  "fields": ["assistant_response", "conversation_history", "cwd", "hook_event_name", "model", "platform", "profile", "session_id", "task_id", "telemetry_schema_version", "tool_input", "tool_name", "turn_id", "user_message"],
  "elapsed_ms": 0.4
}
```

- 阶段 0 结论：`post_llm_call` 每回合提供最终可见回复原文、本回合用户消息、稳定 `session_id`/`turn_id`、本会话更早消息（`conversation_history` 非空）；同步执行、失败不阻塞；中断回合拿不到正文
- 是否满足实现适配器的前提：满足

## 3. Hermes 适配器

- 文件路径：`工具/hooks/hermes_post_turn.py`（112 行）
- 注册位置：`~/.hermes/config.yaml` → `hooks.post_llm_call` → `python3 /Users/apple/Documents/Goodidea/工具/hooks/hermes_post_turn.py`，`timeout: 15`；`hermes hooks list` = 1 条、`✓ allowed`。接入方式写在 `工具/README.md`（只写接入，不写产品规则）
- 读取的 Hermes 字段：`hook_event_name`、`session_id`、`extra.turn_id`、`extra.user_message`、`extra.assistant_response`（Hermes 专有字段只出现在本文件）
- 过滤掉了哪些内容：工具调用、思考过程、系统内容一律不读不写；只取可见文本，空文本跳过
- 调用的通用命令：`python3 工具/capture.py status --session <id>`；`python3 工具/capture.py append --session <id>`（stdin 传事件数组 `[{speaker,time,text,turn_id}]`，说话人 `松海`/`Agent`，时间 `YYYY-MM-DD HH:MM`）
- stop 收尾逻辑：`status` 为 `closing` 时，本次 append 注销 session 后再查一次 `status`，确认已注销才调 `python3 工具/commit.py <记录路径> --message "capture: <记录标题>"`
- 失败日志位置：`.goodidea/tools.log`（追加行含 session、退出码、stderr 摘要）；始终向 stdout 输出 `{}`
- 失败是否阻塞回复：否（实测：提交失败时适配器返回 1，回复照常）

验收结果：

| 场景 | 结果 | 证据 |
|---|---|---|
| 新建捕捉 | 通过 | 沙箱 A：`capture.py start` 后一回合自动转录 2 条（用户 + Agent） |
| 续接已有记录 | 通过 | 续接回合后 `turns=4`；实机联调 3 回合 2→4→注销 |
| 同一事件重复触发 | 通过 | 同一载荷再跑一次仍 2 条（按「时间＋说话人＋原文」去重） |
| 重放同一批事件 | 通过 | `--resume` 重放后文件哈希不变 |
| 删除 `.goodidea/` 后重放 | 通过 | 删状态目录 → `--resume` 重放 → 文件哈希不变 |
| stop 所在回合 | 通过 | 最后 2 条落盘（共 4 条）→ session 注销 → 自动提交（沙箱 `e92cea4`／实机联调 `b104c19`，提交只含该记录） |
| 禁用适配器后的手工退路 | 通过 | 未登记 session 静默返回 0；对照运行（钩子指向空操作脚本）用 `capture.py append` 手工写满 3 回合、`turns=6` |
| 提交失败时仍能写入记录 | 通过 | 存在其他已暂存改动时 `commit.py` 返回 4 → 适配器返回 1、写 tools.log、6 条消息仍落盘 |

## 4. Skill 副本清理

- `~/.hermes/skills/` 是否存在独立 Good Idea Skill：是（部分）
- 发现的路径：
  - `~/.hermes/skills/goodidea/goodidea-ops/`（真实目录，SKILL.md 7.2 KB + references/ + scripts/）
  - `~/.hermes/skills/goodidea/goodidea-source-sync/`（真实目录，SKILL.md 9.8 KB + references/ + scripts/）
  - 6 个失效符号链接：`goodidea-capture-flash`、`goodidea-connect-cards`、`goodidea-form-permanent`、`goodidea-lint`、`goodidea-record-literature`、`goodidea-review-process`，目标 `/Users/apple/Documents/Claude/good-idea/.agents/skills/<name>/`（已不存在）
  - `~/.hermes/skills/.archive/goodidea-project-dev`（归档）
- 处理方式：`goodidea-capture`／`goodidea-distill` 未发现独立副本，仓库 `.agents/skills/` 版本即加载源；`goodidea-ops`／`goodidea-source-sync` 仓库无对应版本 → 未改动（不新建仓库技能目录）；6 个失效链接未改动（同名技能现仅存在于旧克隆 `/Users/apple/Documents/good-idea`，指向它等于把本机环境绑到被弃用的 main 分支）
- 是否确认 Hermes 加载仓库版本：是。验证方式：临时移走 `.agents/skills/goodidea-capture` → `hermes skills list` 中该技能消失（grep 计数 0）→ 放回 → 恢复（计数 1）；`git status` 复原无残留

## 5. 阶段 4：来源同步与标签体检

- 旧 `sync_source.py` 路径：`~/.hermes/skills/goodidea/goodidea-source-sync/scripts/sync_source.py`（944 行，旧副本已删除）
- 迁入后的路径：`工具/sync_source.py`（967 行）
- 是否只做迁移和路径修正：是，仅三处最小改动——① `DEFAULT_ROOT` 改为 `Path(__file__).resolve().parents[1]`；② 删除 5 个硬编码阈值常量，改由 `recognition_params()` 读清单；③ 体检子命令改名 `tags-health`（`tags` 保留同义词）。未扩写产品行为
- 标签校验实现：`check_tags()`——树外标签告警、`停用/已并入` 标签告警
- 标签体检命令：`python3 工具/sync_source.py tags-health [--json]`
- 是否检查树外、停用、已并入标签：是（三者都覆盖）
- 是否做清单结构自检：是（`manifest_problems()`：缺名称、重名、子标签缺父级、文件读不到）
- 是否输出使用次数和最近使用日期：使用次数**有**；最近使用日期**没有**（`tags-health` 输出键为 `mode/records/nodes/counts/retired/problems/untracked/unused_nodes/items/warnings/elapsed_s`，无任何日期字段；方案未要求新增，故未扩写）
- 炒饭会阈值原位置：脚本常量 `KW_DEFAULT/WEEKDAY_DEFAULT/MIN_HOUR_DEFAULT/MIN_MINUTES_DEFAULT/EXCLUDE_TYPE_DEFAULT` + `规则/默认规则.md` 正文重复写数值
- 炒饭会阈值迁移后位置：`规则/标签清单.json`「炒饭会」节点的 `识别参数`（关键词／星期 2／起始小时 17／最短时长分钟 20／排除内容类型）；`默认规则.md` 改为引用该字段
- 是否存在重复阈值：否——`grep` 确认数值只出现在 `规则/标签清单.json`；历史决策记录里的旧记述保留（决策记录是追加式历史，不参与现行裁决）

验收结果：

| 场景 | 结果 | 证据 |
|---|---|---|
| 正常来源登记 | 通过 | 未登记材料 `1922051292205873960` → `sync --dry-run` 返回 `status=dry_run` 并给出目标路径；已登记材料 `1922408121309944784` → `status=exists` 并指回既有记录（去重生效） |
| 标签告警 | 通过 | `sync <id> --dry-run --tag 不存在的标签` → 「标签不在 规则/标签清单.json 中：['不存在的标签']」 |
| 清单结构错误检测 | 通过 | 临时根放入重名／缺名称／子标签缺父级 → 三条全部报出；真清单未改动 |
| 删除 Hermes 旧副本后运行 | 通过 | `tags-health` 退出码 0（29 份来源、7 节点、问题空）；cron 包装脚本 `~/.hermes/scripts/goodidea_weekly_sync.py --dry-run --scan-only` 退出码 0 |
| 删除脚本后手工退路 | 通过（文档层） | `工具/README.md` 与技能写明：取料不可用时按 `数据/模板/source.md` + `python3 工具/new.py source "<标题>"` + `commit.py` 手工完成；未做实机演练 |
| 阈值未散落 | 通过 | `grep -rn "17:00\|≥ 20 分钟\|MIN_HOUR"` 在规则正文与脚本中均无命中，数值只在 `规则/标签清单.json` |

## 6. 正式性能测量

日志与筛选：

- 日志路径：`~/.hermes/logs/agent.log`
- 筛选命令（`<sid>` 为会话 id）：`grep "conversation turn: session=<sid>"`、`grep "API call #" | grep <sid>`、`grep "tool_executor: tool" | grep <sid>`、`grep "Turn ended:" | grep <sid>`、`grep "CLI cleanup" | grep <sid>`
- 样本选择方式：两个沙箱克隆里跑同一结构的 3 回合捕捉（`start` → 续接 → `stop`），同机、同模型 `deepseek-flash`、同 `capture.py`/`commit.py` 版本；改前＝钩子指向空操作脚本（适配器停用，模型逐轮手工 `append`），改后＝钩子指向适配器。回合边界＝`conversation turn` 到 `CLI cleanup`；模型调用＝`API call #` 行数；输出 token＝`out=` 字段；工具调用＝`tool_executor: tool … completed` 行数（每个工具执行一行）

| 场景 | 样本 | 模型调用次数 | 输出 token | 墙钟时间 | 手工 append | 自动落盘 |
|---|---:|---:|---:|---:|---|---|
| 改前 | 1 | 8 | 7448 | 38.0 s | 是（2 条） | 否 |
| 改前 | 2 | 5 | 4340 | 23.0 s | 是（2 条） | 否 |
| 改前 | 3 | 4 | 893 | 7.0 s | 是（2 条） | 否 |
| 改后 | 1 | 2 | 545 | 4.0 s | 否 | 是 |
| 改后 | 2 | 1 | 22 | 2.0 s | 否 | 是 |
| 改后 | 3 | 2 | 881 | 7.0 s | 否 | 是 |

改前会话：`20260926_220931_699f9c`（停在 `closing`、记录 uncommitted）；改后会话：`20260926_220313_d42a99`（stop 回合自动提交）。

统计：

- 改前平均：22.7 s（中位 23.0 s；模型调用 5.7 次/回合；输出 4227 token/回合）
- 改前中位数：23.0 s
- 改后平均：4.3 s（中位 4.0 s；模型调用 1.7 次/回合；输出 483 token/回合）
- 改后中位数：4.0 s
- p95（如有）：未计算（每组 3 个样本，分位数无意义）
- 调用次数变化：17 → 5 次（3.4×）
- 输出 token 变化：12,681 → 1,448（8.8×）
- 墙钟时间变化：68.0 s → 13.0 s（5.2×）；工具调用 16 → 2 次（8.0×）；手工 append 6 次 → 0 次
- 固定加载清单收益：无法判断（该项优化本机未实施，没有前后样本）
- 自动转录收益：即上表改前/改后之差；两组都在 22:03–22:11 同一时段，缓存命中 99–100%，无重试/失败行，无异常样本（唯一慢点来自模型延迟：改前回合 1 延迟 35.9 s、墙钟 38.0 s）
- 是否存在缓存、降速窗口或异常样本：无降速窗口异常；改前组的用户消息本身更长（手工流程必须把补写指令写进提示），输出 token 的倍差里含这部分固有差异

## 7. Git 提交

| 阶段 | Commit | 提交说明 |
|---|---|---|
| 阶段 0 | `5c0f37f` | 探针：核实 Hermes 回合事件契约 |
| 适配器 | `b91d2c8` | feat: 增加 Hermes 回合后捕捉适配器 |
| Skill 副本清理 | `cf29fd4` | chore: 对齐 Hermes Skill 副本（空提交，仅报告处理结果） |
| 阶段 4 | `03a3345` | feat: 迁入来源同步与标签体检能力 |
| 性能测量 | `9436de0` | docs: 补充真实性能测量结果 |

- 是否有未提交改动：有 1 处且与本次无关——`M .obsidian/workspace.json`（7 增 7 删，仅 `lastOpenFiles`；按松海指示豁免，未提交、未 stash、未 checkout）
- 是否误改 `.obsidian/workspace.json`：否（每次提交都只 `git add` 自己新建/改动的文件）
- 是否修改 main：否
- 是否 push：否（本次报告落盘后由松海指示 push）

## 8. 未完成项与风险

- 未完成项：
  1. 桌面端（`hermes serve`，9-24 启动）未重启 → 桌面会话的自动转录尚未生效（授权已在 allowlist，重启即注册）
  2. `固定加载清单` 优化本机未实施 → 该项收益无数据
  3. 「删除脚本后手工退路」只做了文档层核对，未做实机演练
  4. 未验证：provider 彻底失败且无回落时的回合行为（模型 400 被回落接管，未造出真失败）
- 暂停原因：无（无因缺证据而暂停的阶段；唯一中断是 3 秒睡眠对照实验，属计划内）
- 需要松海决定的事项：
  1. 桌面端是否现在重启以启用桌面会话的自动转录
  2. `goodidea-ops`、`goodidea-source-sync` 两份技能是否迁入仓库 `.agents/skills/` 后改符号链接（当前仓库无对应版本）
  3. `~/.hermes/skills/goodidea/` 下 6 个失效符号链接：保留／删除／不管
  4. 是否要 `tags-health` 补「最近使用日期」（当前只有使用次数，新增属功能扩展）
- 不能确认的结论：
  1. 「提交失败时 `capture.py status` 显示未提交」与实测有差异：失败发生在 append 注销之后，session 已不存在，`status --session` 只能报 `active=false`，未提交事实需从 git 工作区看；`closing` 未提交时 `status` 的 `uncommitted` 字段为 `true`（已在真实仓库观察到）。若要求 `status` 直接可见，需改 `capture.py`，超出适配器职责
  2. 性能表只覆盖 CLI 面、每组 3 样本，只作方向性证据，不作长期指标
  3. 中断回合的可见消息不会被自动转录（阶段 0 实测），只能手工补写——这是已知缺口，不是缺陷
